# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=*

# Database Configuration
# For SQLite (default):
DB_ENGINE=django.db.backends.sqlite3
# DB_NAME is optional for SQLite, defaults to db.sqlite3

# For PostgreSQL (uncomment and configure):
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=evaluator_db
# DB_USER=your_user
# DB_PASSWORD=your_password
# DB_HOST=localhost
# DB_PORT=5432

# CORS Settings
CORS_ALLOW_ALL_ORIGINS=True
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000,http://127.0.0.1:3000,http://127.0.0.1:8000

# Email Settings
# For development (console backend):
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# For production (SMTP):
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password

# Contact Information
DEFAULT_FROM_EMAIL=Chelsea Corporate <noreply@chelseacorporate.com>
CONTACT_EMAIL=info@chelseacorporate.com
CONTACT_PHONE=(0) 20 3011 1373
SITE_URL=https://chelseacorporate.com
